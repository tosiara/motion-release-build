name: buildmotions.sh
on:
  workflow_dispatch:

env:
  VERSION: 4.7
  EMAIL: tosiara@users.noreply.github.com

jobs:
  buildmotions:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: entrypoint.sh
        run: |
             docker info
             sudo docker run --platform linux/arm64/v8 registry.hub.docker.com/arm64v8/ubuntu:jammy uname -a
             #sudo -E ./buildmotions.sh
      - name: Save debs
        uses: actions/upload-artifact@v4
        with:
          name: debs
          path: ./*.deb
